    <div>
        <label for="{{ $id }}" class="block mb-1 text-sm font-medium text-gray-900">{{ $label }}</label>
        <textarea name="{{ $name }}" id="{{ $id }}" placeholder="{{ $placeholder }}"
            class=" border border-[#DEE2E6] text-gray-900 text-sm rounded-md focus:ring-0 focus:border-primary block w-full p-2.5 h-[86px] "
            required></textarea>

    </div>
